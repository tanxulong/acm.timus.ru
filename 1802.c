/*
 * acm.timus.ru
 * 1802 Ural Steaks
 */
#include <stdio.h>
#include <stdlib.h>

int main()
{
	int n, k;
	scanf("%d %d", &n, &k);
	if(n < 0 || n > 1000 || k < 0 || k > 1000)
	{
		return EXIT_FAILURE;
	}

	
}
